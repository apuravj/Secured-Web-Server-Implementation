# Secured-Web-Server-Implementation
 Designed and deployed a secured AWS VPC with the public (DMZ) and private subnets deploying a public EC2 web server and a private RDS database with restricted access via NAT Gateway, IAM, security groups.
